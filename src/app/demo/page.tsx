import DemoPageClient from '@/components/sections/demo-page-client';

export const metadata = {
  title: 'SMART BI Agent — Live Demo',
  description: 'Ask your business data anything. Live AI agent demo powered by SMART BI.',
};

export default function DemoPage() {
  return <DemoPageClient />;
}
